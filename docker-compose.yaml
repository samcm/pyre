version: '3.8'

services:
  pyre:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./config.yaml:/app/config/config.yaml:ro
      - ./data:/app/data
    environment:
      - TZ=UTC
    restart: unless-stopped
